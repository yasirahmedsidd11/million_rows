services:
  shipping-api:
    build: .
    ports:
      - "3000"
    restart: unless-stopped
    environment:
      PORT: "3000"
      CSV_PATH: /app/data/rows.csv
    volumes:
      - csv_data:/app/data

volumes:
  csv_data:
